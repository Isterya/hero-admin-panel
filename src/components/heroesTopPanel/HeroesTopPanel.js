import './heroesTopPanel.scss';

const HeroesTopPanel = () => {
   return (
      <header className="heroes-header">
         <p className="heroes-header__greeting">Привет, пользователь.</p>
      </header>
   );
};

export default HeroesTopPanel;
